{
  "name": "stromboard-frontend",
  "version": "1.0.0",
  "description": "Frontend for the collaborative whiteboard application",
  "main": "index.js",
  "scripts": {
    "start": "lite-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "socket.io-client": "^4.7.2"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}